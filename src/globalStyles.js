import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerTitle: {
    color: 'white',
    fontSize: 20,
  },
  card: {
    backgroundColor: "white",
    margin: 15,
    marginBottom:0,
    padding:10,
  }
});

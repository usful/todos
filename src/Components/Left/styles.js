import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  left: {
    flex: 1,
    flexDirection: "row",
    justifyContent: 'flex-start',
    alignItems: 'center',
    paddingLeft: 5,
  },
});

import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  checkbox: {
    borderWidth: 1,
    borderRadius: 5,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width:20,
    height:20,
  }
});
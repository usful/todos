import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  right: {
    flex: 1,
    flexDirection: "row",
    justifyContent: 'flex-end',
    alignItems: 'center',
    paddingRight: 5,
  },
});
